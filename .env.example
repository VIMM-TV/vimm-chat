# Server Configuration
PORT=3000
NODE_ENV=development

# Database Configuration
DB_DIALECT=sqlite      # Can be 'postgres' or 'sqlite'
DB_HOST=localhost
DB_PORT=5432
DB_NAME=vimm_chat
DB_USER=postgres
DB_PASS=postgres

# SQLite Configuration (for testing)
# Comment out next line if using PostgreSQL
SQLITE_PATH=./vimm-chat.sqlite

# VIMM Core API
VIMM_CORE_API_URL=http://localhost:3000/api

# CORS
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080

# Socket.IO Configuration
SOCKET_PATH=/socket.io